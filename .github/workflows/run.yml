name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Run echo command
      run: |
        echo "USER" | openssl enc -e -aes-256-cbc -a -pbkdf2 -iter 10000 -k zhxsjxq
